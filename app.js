const state={role:'guest'};function go(role){state.role=role;localStorage.setItem('ss_role',role);document.querySelectorAll('[data-role-panel]').forEach(x=>x.hidden=x.dataset.rolePanel!==role);document.querySelectorAll('[data-role-btn]').forEach(x=>x.classList.toggle('ghost',x.dataset.roleBtn!==role));}function initRole(){go(localStorage.getItem('ss_role')||'guest')}document.addEventListener('click',e=>{const t=e.target; if(t.matches('[data-role-btn]'))go(t.dataset.roleBtn); if(t.matches('[data-open]'))alert('Opening '+t.dataset.open); if(t.matches('[data-priority]'))alert('Priority placement opened'); if(t.matches('[data-book]'))alert('Booking flow started'); if(t.matches('[data-search]'))alert('Searching for '+(document.querySelector('[data-search-input]')?.value||'all'));});initRole();